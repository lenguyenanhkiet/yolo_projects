# --- Set mặc định cho text file ---
* text=auto

# --- Python script và shell script dùng LF ---
*.py text eol=lf
*.sh text eol=lf

# --- Batch script (Windows) dùng CRLF ---
*.bat text eol=crlf

# --- Jupyter notebook giữ nguyên ---
*.ipynb binary

# --- File nhãn YOLO giữ nguyên xuống dòng như ban đầu ---
*.txt text eol=lf

# --- Hình ảnh là binary ---
*.jpg binary
*.jpeg binary
*.png binary

# --- YAML giữ LF (cho cấu hình yolo/data.yaml) ---
*.yaml text eol=lf
*.yml text eol=lf

# --- Loại bỏ carriage return trên các file cấu hình ---
*.json text eol=lf
*.xml text eol=lf

# --- Không theo dõi thay đổi line-ending của các file này ---
.idea/* text eol=lf
